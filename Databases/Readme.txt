DO NOT modify the files inside the folder manually , if u dont know what u are doing 

This folder contains the db files , each corresponding to a user profile .
If u change the name of the db file the profile name also changes.
If  u delete the files the user profiles are also deleted